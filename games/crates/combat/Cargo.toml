[workspace]

[package]
name = "game-combat"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
ssbm_utils = "=0.4.0"

[dev-dependencies]
serde_json = "1"
