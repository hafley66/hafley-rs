[workspace]

[package]
name = "rollback"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
ggrs = "=0.13.0"
serde = { version = "1", features = ["derive"] }
