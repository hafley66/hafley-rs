# Lane: per-turn cwd on agent_turn (hafley-rs)

Favor plain code. If reality deviates from this brief, STOP and write REPORT.md at the worktree root describing the deviation; do not improvise.

## Issue
Read `issues/boop-turn-cwd/item.md` in $PWD first. Its Expected and Acceptance sections are the spec.

## Files you own (inside $PWD, your worktree)
- crates/boop-store/src/ident.rs: schema v27 (`ALTER TABLE agent_turn ADD COLUMN cwd_id INTEGER`, add the `if self.schema_version()? < 27` block after the v26 block at ~:812, bump `SCHEMA_VERSION` at :39), `add_turn` gains `cwd: Option<&str>` (:951) and interns it through `self.intern("dict_cwd", ..)` like `upsert_session_row` does at :935, every `add_turn` caller (:2927, :3027, :3075, :3086, :3104) passes the record's cwd, `CREATE VIEW IF NOT EXISTS v_turn_cwd` in the schema DDL near the `agent_turn` table (:3449 area) with `COALESCE(t.cwd_id, s.cwd_id)` joined to `dict_cwd`.
- crates/boop-store/src/ident.rs tests module: the three tests from the issue's Acceptance list. Put fixtures under crates/boop-store/tests/fixtures/ if a transcript file is needed; a claude fixture is two records with different `cwd` values.
- issues/boop-turn-cwd/item.md: tick the acceptance boxes you met, set `status: fixed`, add a `## Tests Run` section with the pasted output.

Do not touch any other file. Do not touch boop-harness. Do not touch `crates/boop-store/src/query.rs`.

## How the cwd reaches project_line
`project_line` (:2994) parses each transcript line into `serde_json::Value`. For claude the record has a top-level `cwd` string. For codex the projected object is a `turn_context` record whose `payload.cwd` carries the turn's cwd; if the codex path never reaches `add_turn` through `project_line`, pass `None` for codex and say so in the REPORT. Kimi and opencode pass `None`.

## Rules
1. No backfill scan of existing turns on migrate. NULL plus the COALESCE view is the answer.
2. `DOCTRINE` or any other string that names the schema version must match 27; grep for `26` in ident.rs and doctrine files before finishing.
3. Banned identifiers: provenance, substrate, load-bearing, regime.
4. One commit, subject exactly: `boop-store: agent_turn.cwd_id per turn, v_turn_cwd view, schema v27`.

## Validation (run all, paste output into REPORT.md)
```
export CARGO_TARGET_DIR=$HOME/.cache/cargo-target/turn-cwd
cargo test -p boop-store 2>&1 | tail -20
cargo clippy -p boop-store --all-targets -- -D warnings 2>&1 | tail -5
cargo build -p boop 2>&1 | tail -3
git log --oneline -1
```
Write REPORT.md at the worktree root: files changed, test counts before and after, the deviation list (empty if none).
