# API reference

The full rustdoc for every workspace crate is generated into the site's `api/`
directory.

[Open the API reference](api/index.html)

The landing page is generated from the workspace membership and the targets
`cargo doc` actually emitted, so it lists exactly the crates this build
documented. Each entry links to that crate's rustdoc with its search index and
assets.
