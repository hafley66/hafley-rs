[package]
name = "boop-turnvis"
version = "0.1.0"
edition = "2021"

[dependencies]
aho-corasick = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# The snapshot a host hands the matcher: rows, wrap flags and viewport size.
boop-mux = { version = "0.0.9", path = "../boop-mux" }
