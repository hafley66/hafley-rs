{
  "case": "kotlin-ambiguous-receiver",
  "answer": [
    "tests/fixtures/kotlin_receivers/lib.kt:323-342"
  ],
  "notes": "h.w.run() in fieldLeg. Holder.w is typed Widget, so the target is Widget.run (lib.kt:9). Decoy.run (lib.kt:23) shares the bare name and sits at a different span."
}
