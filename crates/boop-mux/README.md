# boop-mux

The tmux multiplexer seam `boop` drives: one `Multiplexer` trait, one `Tmux`
implementation over `tmux_interface` and raw `tmux` spawns.

## License

Dual licensed under MIT or Apache-2.0, at your option.
